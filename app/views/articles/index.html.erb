<% if user_signed_in? %>
  <%= render partial: 'articles/form', locals: { instance: current_user } %>
<% end %>

<%= render partial: 'articles/content',
           locals: { articles: @articles } %>

<%= paginate @articles %>
